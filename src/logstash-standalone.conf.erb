input {
    file {
        type => "elasticsearch"
        path => [
            "<%= Dir.getwd %>/var/log/elasticsearch/elasticsearch.log"
        ]
    }
}

output {
    #stdout { debug => true debug_format => "json"}
    elasticsearch { }
}
